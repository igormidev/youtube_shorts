include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    invalid_annotation_target: ignore

  language:
    strict-casts: false

  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "example/video_downloader_flutter/"
    - "lib/youtube_explode_fork/**" 